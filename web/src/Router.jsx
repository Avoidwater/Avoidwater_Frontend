import { Router, Routes, Route } from "react-router-dom";

const AppRouter = () => {
    return(
        <Router>
            <Routes>
                
            </Routes>
        </Router>
    )
}

export default AppRouter;